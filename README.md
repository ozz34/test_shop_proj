Python 3.14, Pytest 9.0.2, Selenium 4.40.0.  
Basket page implemented in Product page, Main Page implemented in Base Page.  
Create and activate environment: python3 -m venv sel_env -->  
install requirements: pip install -r requirements.txt -->  
launch command: pytest -v --tb=line --language=en -m need_review.  
To launch test's USE VPN.  
If catch CONNECTION ERROR - repeat test's and check VPN CONNECTION.
